 <!-- <view class="section__title">省市区选择器</view>
  <picker mode="region" bindchange="bindRegionChange" value="{{region}}" custom-item="{{customItem}}">
    <view class="picker">
      当前选择：{{region[0]}}，{{region[1]}}，{{region[2]}}
    </view>
  </picker> -->

<!-- <button bindtap="click">点击隐藏map</button>
<map id="map" wx:if="{{isShow}}" longitude="104.07" latitude="30.67" scale="14" controls="{{controls}}" bindcontroltap="controltap" markers="{{markers}}" bindmarkertap="markertap" polyline="{{polyline}}" bindregionchange="regionchange" show-location style="width: 100%; height: 300px;"></map> -->

<swiper indicator-dots="{{indicatorDots}}" circular="true" previous-margin="35px"
next-margin="35px"
        autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}">
        <block wx:for="{{imgPath}}" wx:key="*this">
          <swiper-item>
            <!-- <view  class=" box">swiper-item {{item}}</view> -->
            <image src="{{item}}"></image>
          </swiper-item>
        </block>
</swiper>





















